export const metadata = {
  title: 'Array - Sequence',
  description: 'Generate Array Sequence',
}

export default function ArraySequenceLayout({ children }) {
  return (
    <>
      {children}
    </>
  )
}
